{
  "name": "image-craft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "image-craft": "dist/src/cli.js",
    "image-craft-mcp": "dist/src/mcp.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
